#include "main.h"

/**
 *malloc_checked - memory allocator
 *
 *@b: string
 *
 * Return: returns pointer to address
 */
void *malloc_checked(unsigned int b)
{
	void *a;

	a = malloc(b);
	if (a == NULL)
		exit(98);
	return (i);

}
